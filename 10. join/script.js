const fruits = ['Banana', 'Orange', 'Apple', 'Mango'];

/**
 * -THE JOIN METHOD-
 * the join method joins all array elements into a string
 * its like the toString method, but you can specify your
 * own seperator
 */
document.querySelector('#demo').innerHTML = fruits.join('&emsp;');
